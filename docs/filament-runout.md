# Filament Runout

Signal: `PA4 / Filament Detection 1`.

The feature is enabled by default. Marlin executes `M600` through Advanced Pause when runout is detected. Runtime control is grouped under `Configuration > KP3S Setup > Sensors / IMU > Filament Sensor`.
