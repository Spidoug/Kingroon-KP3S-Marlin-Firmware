# Filament Runout

Signal: `PA4 / Filament Detection 1`.

The feature is enabled by default. Marlin executes `M600` through Advanced Pause when runout is detected.

Runtime control remains in Marlin's native `Configuration` menu as `Filament Runout`. It is not duplicated inside the MPU / sensor menu.
